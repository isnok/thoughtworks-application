upcoming release:
=================

setup
    - secure vagrant access!!
    - reduce sudo
    - ansible handlers & ...?

load-balancing
    - introduce ansible variables
    - scale all up to 2 hosts
    - fine-tune configs

deployment plan
    - deploy via ansible (if possible)
    - else fabric


further development:
====================

log-setup
    - self-hosted vs cloud

monitoring
    - availability-monitoring
    - service-quality-monitoring

use a DBMS
    - think about future plans
        + data flow in envisioned full-scale product
        + self-hosted vs cloud
    - check NoSQL possibilities
    - then choose wisely

scaling / rolling releases


big rollout:
============

CDN
global availability
