papertrail
==========
    - list dependencies (virtualbox, vagrant, ansible)



upcoming release:
=================

setup
    - reduce sudo
    - ansible handlers & ...?

load-balancing
    - fine-tune configs

deployment plan
    - deploy via ansible (if possible)


further development:
====================

log-setup
    - self-hosted vs cloud

monitoring
    - availability-monitoring
    - service-quality-monitoring
    - external alerting service

consider a (R)DBMS
    - think about data flow in the app

scaling / rolling releases


big rollout:
============

CDN
global availability
